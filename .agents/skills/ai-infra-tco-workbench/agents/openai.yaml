interface:
  display_name: "Ai Infra Tco Workbench"
  short_description: "Repo-specific patterns and workflows for ai-infra-tco-workbench"
  default_prompt: "Use the ai-infra-tco-workbench repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true